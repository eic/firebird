name: Frontend CI Workflow

on:
  push:
    branches:
      - '*'
  pull_request:
    branches:
      - master

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./firebird-ng
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: false
          submodules: recursive
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install Dependencies
        # npm workspaces: single lockfile at the repo root
        working-directory: .
        run: npm install

      - name: Run Tests
        run: npm run test:headless

  build:
    needs: test
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./firebird-ng
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: false
          submodules: recursive
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install Dependencies
        # npm workspaces: single lockfile at the repo root
        working-directory: .
        run: npm install

      - name: Build
        run: npm run build

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: firebird-ng-build
          path: ./firebird-ng/dist/
